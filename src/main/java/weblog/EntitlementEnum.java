package weblog;

public enum EntitlementEnum {
	VIEW,		// User can view the logbook (including view the map)
	ADD,		// User can add entries to the log
	UPDATE,		// User can update existing log entries
	DELETE		// User can delete existing log entries
}
